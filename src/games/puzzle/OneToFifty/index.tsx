import styled from "styled-components";

import GameWindow from "../../GameWindow";

function OneToFifty() {
  return (
    <GameWindow>
      <Container>OneToFifty</Container>
    </GameWindow>
  );
}

const Container = styled.div``;

export default OneToFifty;
