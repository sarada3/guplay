import styled from "styled-components";

import GameWindow from "../../GameWindow";

function FlipCard() {
  return (
    <GameWindow>
      <Container>FlipCard</Container>
    </GameWindow>
  );
}

const Container = styled.div``;

export default FlipCard;
